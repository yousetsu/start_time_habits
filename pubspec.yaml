name: start_time_habits
description: Start time habituation

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_picker: ^2.0.5
  intl: ^0.17.0
  sqflite: ^2.2.1
  path: ^1.8.2
  cupertino_icons: ^1.0.2
  flutter_calendar_carousel: ^2.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/external_assets.db


